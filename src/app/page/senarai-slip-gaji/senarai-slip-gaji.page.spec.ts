import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SenaraiSlipGajiPage } from './senarai-slip-gaji.page';

describe('SenaraiSlipGajiPage', () => {
  let component: SenaraiSlipGajiPage;
  let fixture: ComponentFixture<SenaraiSlipGajiPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SenaraiSlipGajiPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
